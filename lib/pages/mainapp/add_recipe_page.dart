import 'package:flutter/material.dart';

class AddRecipePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('AddRecipePage'),
      ),
    );
  }
}
